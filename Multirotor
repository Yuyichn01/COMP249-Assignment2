package thirdPackage;

import secondPackage.Helicopter;

public class Multirotor extends secondPackage.Helicopter {
	//Number of rotors of multirotor
	public int noOfRotors;
	
	//default constructor
	public Multirotor() {
		noOfRotors = 0;
	}
	
	//copy constructor
	public Multirotor(Multirotor multirotor) {
		super(multirotor.noOfCylinders, multirotor.creationYear, multirotor.passengerCapacity);
		this.noOfRotors = multirotor.noOfRotors;
	}
	
	//parameter constructor
	public Multirotor(int nORotors) {
		this.noOfRotors = nORotors;
	}
	
	//getter of noOfRotors
	public int getNoOfRotors() {
		return this.noOfRotors;
	}
	
	//setter of noOfRotors
	public void setNoOfRotors(int nORotors) {
		this.noOfRotors = nORotors;
	}
	
	//toString method
	public String toString() {
		return ("This Multirotor is manufacutured by " + super.Brand + " in " + super.creationYear 
		+". It costs $" + super.price + " with a " + super.horsepower + " horsepwoer. "
		+ "It has " + super.noOfCylinders + " cylinders and " + this.noOfRotors + " rotors, and passenger capacity is "
		+ super.passengerCapacity);
	}
	
	//equals method
	public boolean equals(Multirotor multirotor) {
		return (super.equals(multirotor) && this.equals(multirotor));	
	}
}
